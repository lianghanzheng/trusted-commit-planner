# MigMaster

> AI 辅助编译器开发方法论工具包。AI 生成，人蒸馏。

---

## 一、环境配置

以下路径由 `/setup` 命令维护，请勿手动编辑。未配置的项会在依赖它的命令中报错并引导你完成配置。

```
LLVM_BUILD={{LLVM_BUILD}}           # LLVM 构建目录 (含 bin/opt, bin/llvm-extract 等)
FILECHECK={{FILECHECK}}             # FileCheck 二进制路径 (默认 ${LLVM_BUILD}/bin/FileCheck)
LLVM_EXTRACT={{LLVM_EXTRACT}}       # llvm-extract 二进制路径 (默认 ${LLVM_BUILD}/bin/llvm-extract)
LLVM_REDUCE={{LLVM_REDUCE}}         # llvm-reduce 二进制路径 (默认 ${LLVM_BUILD}/bin/llvm-reduce)
PYTHON={{PYTHON}}                   # Python 3.8+ 解释器
CLANG={{CLANG}}                     # clang 二进制路径 (用于 IR 生成)
TEST_SUITE_DIR={{TEST_SUITE_DIR}}   # 端到端测试套件目录 (可选)
```

> 使用 `/setup` 命令修改以上配置。例如：`/setup LLVM_BUILD=/opt/llvm-20/build`

---

## 二、工程技术规范

### 2.1 Commit 规范

以下规范提炼自 LLVM、GCC、Triton、IREE 的贡献者指南。完整原则见 `docs/Principle/COMMIT_PRINCIPLES.md`。

**绝对规则：**

| 规则 | 要求 |
|------|------|
| 原子性 | 一个 commit 只做一件事。禁止混入无关重构或格式化。 |
| 规模上限 | 净增代码 ≤ 200 行。超过此阈值应在 Plan Mode 中规划拆分。 |
| 独立验证 | 每个 commit 必须可独立编译并通过测试。 |
| 强制测试 | 所有代码变更必须附带测试（正面/负面/边界）。 |
| 提交信息 | 标题 ≤ 75 字符，以组件标签开头（如 `[MyPass]`）。正文解释 WHY。 |

**禁止事项：**
- 禁止在 commit message 或 PR 描述中 `@` 提及他人
- 禁止同一个 commit 混合功能性改动和 NFC（格式化、重命名）
- 禁止提交调试代码（`errs()`、`printf`、`FIXME` 等）

### 2.2 测试分层策略

完整原则见 `docs/Principle/TESTING_PYRAMID.md`。

```
功能性测试 (tests/features/)      — 对功能的"定义"。IR 简洁、教学式。可随设计演变修改。
回归测试   (tests/regressions/)   — 对现实复杂性的"固化"。从真实 IR 缩减而来。绝不随意修改。
端到端测试 (独立仓库或 CI)         — 完整程序编译+执行。周期性运行，作为最终安全网。
```

**关键原则：功能性测试是活的规范，回归测试是死守的防线。两种测试必须共存且分类管理。**

### 2.3 人机分工

| 谁 | 职责 |
|----|------|
| **开发者（你）** | 定义"意义"：什么是一个逻辑单元、什么样的测试算通过、何时提交、何时转向。始终掌握对代码意义的最终解释权。 |
| **AI** | 生成代码、草拟提交信息、分析代码结构、建议拆分方案、生成 ADR 和测试骨架、执行重复性检查。 |

**AI 的输出是原材料，不是成品。提交前的 diff 自审是必须项，不是可选项。**

### 2.4 双仓库架构

本项目是**手工库（精品库）**。你应当还有一个 AI 快速交付库作为"试验场"。

```
AI 快速交付库 (试验场)           手工库（本项目）
─────────────────────           ────────────────
允许: 大 Commit, 快速试错       要求: 小 Commit, 规范化测试
操作: AI 自由生成                操作: 人工挑选移植
```

**代码流向**：AI 库 → 提取测试 + 核心逻辑 → 手工库。**单向，不可逆。**

### 2.5 文档策略

**核心原则：文档只记录"为什么"，不记录"怎么做"。**

| 文档类型 | 角色 | 维护 |
|----------|------|------|
| CLAUDE.md | 项目宪法——本文件 | 低频更新 |
| ADR (`docs/decisions/`) | 设计决策记录——为什么选方案 A | 只增不改 |
| 测试用例 | 承载功能细节的"活文档" | 与代码同步 |
| 提交历史 | 承载演进历史 | 精心编写每个 commit message |

**不建议维护**：传统功能说明文档。细节会被代码颠覆，用测试用例替代。

---

## 三、可用命令索引

以下命令均通过 `/命令名` 调用。详细规格见 `docs/FormalDesign-v1.md`。

### 开发流程

| 命令 | 用途 | 适用场景 |
|------|------|----------|
| `/pre-plan` | 任务粒度审查 | 进入 Plan Mode 前，检查需求是否过大 |
| `/micro` | 原子任务执行 | 执行 ≤150 行的单一任务，完成后不自动提交 |
| `/commit-review` | 模拟提交审查 | 提交前一站式检查：规模/测试/message/核查表 |
| `/verify` | 验证命令生成 | 提交后从 lit 测试提取手动验证命令 |

### 测试与分析

| 命令 | 用途 | 适用场景 |
|------|------|----------|
| `/test-gen` | 测试生成 + 抽象降级检测 + IR 缩减 | 为新 Pass 生成四象限测试骨架，检测手写 IR 是否过于理想化 |
| `/split-commit` | Commit 拆分分析 | 将 AI 库的大 commit 拆分为可独立验证的小 commit |

### 知识

| 命令 | 用途 | 适用场景 |
|------|------|----------|
| `/mentor` | 编译器技术问答 | LLVM/MLIR/CUDA/SYCL API、设计模式、调试技巧 |
| `/learn-from` | 学习工作簿生成 | 将仓库 commit 历史转化为带实操实验的学习脚手架 |
| `/archeology` | 仓库考古 | 分析陌生仓库的模块结构和建议阅读路径 |
| `/adr` | 设计决策记录 | 重大设计变更后记录"为什么这么做" |

### 基础设施

| 命令 | 用途 | 适用场景 |
|------|------|----------|
| `/setup` | 描述性配置 | 修改本文件的配置项，无需手动编辑 |
| `/check-turn` | 转向信号检测 | 评估是否到了重新实现的时机（三次原则） |

---

## 四、工作流路由

AI 应在每次对话中主动检查以下规则，在合适的时机建议用户调用下一个命令。

| 当前状态 | 触发条件 | 建议操作 |
|----------|----------|----------|
| 用户描述了一个开发需求 | 需求涉及代码变更 | 建议 `/pre-plan` 评估粒度 |
| `/pre-plan` 通过 | 粒度合适 | 建议进入 Plan Mode |
| Plan Mode 完成规划 | 用户批准计划 | 建议 `/micro` 执行第一步 |
| `/micro` 完成代码变更 | 变更满足要求 | 建议 `/commit-review` |
| `/commit-review` 通过 | 用户决定提交 | 提醒提交后运行 `/verify` |
| 用户手动执行 `git commit` | 提交完成 | 建议 `/verify` |
| `/test-gen` 检测到抽象降级 | ≥ 3 项危险信号 | 引导进入 IR 比对与缩减流程 |
| 与 `/mentor` 讨论技术问题 ≥ 3 轮 | 用户开始总结 | 建议 `/learn-from` |
| 用户提到"移植"、"整理代码" | 涉及 AI 库代码 | 建议 `/split-commit` |
| 用户提到"重构"、"重写"、推倒重来 | 涉及同一模块 | 建议 `/check-turn` |
| 重大设计变更完成 | 用户确认方案 | 建议 `/adr` |

---

## 五、禁止事项

以下行为在任何情况下均不允许：

1. **不自动提交代码。** 始终由开发者在审查后手动执行 `git commit`。
2. **不在手工库中直接运行无约束 AI。** 所有 AI 生成的代码必须先经过 `/micro` 或 `/pre-plan` 的粒度控制。
3. **不跳过测试。** 变更代码后必须运行对应测试。Stop Hook 会提醒，但执行在人。
4. **不在 commit message 中 `@` 任何人。**
5. **不删除回归测试。** `tests/regressions/` 中的测试是历史 bug 的墓碑，只有在该 bug 的修复方案被彻底废弃时才可移除。
6. **不手动编辑本文件。** 使用 `/setup` 命令修改。

---

## 六、快速参考

```
开始开发新功能:
  /pre-plan → Plan Mode → /micro → (审查 diff) → /commit-review → git commit → /verify

从 AI 库移植代码:
  /split-commit <commit-hash> → /micro → /commit-review → git commit → /verify

学习陌生仓库:
  /archeology <path> → /learn-from <path>

技术讨论:
  /mentor (自然对话) → /learn-from → 实操实验 → /adr

检测重构时机:
  /check-turn
```
